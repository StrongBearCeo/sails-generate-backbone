{
  "name": "sails-generate-backbone",
  "version": "0.10.0",
  "description": "sails generate backbone",
  "main": "Generator.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "backbone",
    "generator",
    "sails",
    "generate",
    "plugin"
  ],
  "author": "balderdashy",
  "license": "MIT",
  "dependencies": {
  	"lodash": ">=2.4.x",
  	"merge-defaults": ">=0.1.0"
  },
  "devDependencies": {
    "sails-generate": "*",
    "reportback": "*",
    "fs-extra": "*"
  },
  "sailsGenerator": {
    "type": "backbone",
    "behavior": "overrides `sails generate backbone`",
    "sailsVersion": "~0.10.0"
  }
}
